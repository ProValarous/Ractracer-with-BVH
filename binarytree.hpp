/*
#include <SDL.h>
#include "node.hpp"

struct Node
{
    Node *left;
    Node *right;
    BoundingBox *box;
    bool isLeaf = true;
};

class BinaryTree
{
    private:
        
    
    public:
        Node *root;

        BinaryTree();

        void AddNode(Node *newNode, Node *parentNode,char LR);

        void DeleteNode(Node *node);

        int size(Node *u);

        int height(Node *u);




};

*/
