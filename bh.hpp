/*
#include "binarytree.hpp"
#include "node.hpp"

class BVH
{
    private:
    BoundingBox *root;

    BinaryTree BT();
    




    public:
    BVH(); //make bounding boxes for all object and store in in a arraylist

    void partition(BoundingBox *u, Node *n);

    void sizeResize(BoundingBox *u);




};
*/
